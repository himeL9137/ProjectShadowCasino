1. Fix the "Add New Game" functionality
Ensure that you can input your HTML game code directly into your admin panel. Update the handleAddNewGame function to include an input field for the HTML content of the game:

// New game form state with HTML content
const [newGameForm, setNewGameForm] = useState({
    name: '',
    type: '',
    htmlContent: '',  // Added for HTML game code
    winChance: 50,
    maxMultiplier: 2.0,
    minBet: 1,
    maxBet: 1000,
    description: ''
});
const handleAddNewGame = async () => {
    try {
        setIsAddingGame(true);
        
        // Validate form
        if (!newGameForm.name || !newGameForm.type || !newGameForm.htmlContent) {
            setError('Game name, type, and HTML code are required');
            return;
        }
        
        // Call API to add new game
        await adminApiCall('POST', '/api/admin/games/add', newGameForm);
        
        // Reset form
        setNewGameForm({
            name: '',
            type: '',
            htmlContent: '',  // Reset HTML content
            winChance: 50,
            maxMultiplier: 2.0,
            minBet: 1,
            maxBet: 1000,
            description: ''
        });
        
        setError(null);
        
    } catch (err) {
        setError(err instanceof Error ? err.message : 'Error adding new game');
        console.error('Error adding new game:', err);
    } finally {
        setIsAddingGame(false);
    }
};
2. Create the UI for Games
Use an approach where the newly added games will be displayed similarly to existing ones (Slots, Dice, Plinko). In your games section (e.g., in the homepage component), you can render the games including the new ones:

const [gamesData, setGamesData] = useState([]);
useEffect(() => {
    const fetchGames = async () => {
        const response = await apiRequest('/api/admin/games'); // Fetch all games
        setGamesData(response.data); // Store them in state
    };
    
    fetchGames();
}, []);
// Rendering games
return (
    <div>
        <input 
            type="text" 
            placeholder="Search games..." 
            onChange={e => handleSearch(e.target.value)} 
        />
        <div className="games-list">
            {gamesData.map(game => (
                <GameCard key={game.id} game={game} /> // Assuming GameCard component displays each game
            ))}
        </div>
    </div>
);
// Implement handleSearch to filter games based on user input
const handleSearch = (searchTerm) => {
    const filteredGames = initialGamesData.filter(game => 
        game.name.toLowerCase().includes(searchTerm.toLowerCase())
    );
    setGamesData(filteredGames);
};
3. Embed HTML Games
When displaying HTML games, you can use an iframe or a dedicated component to render the HTML code. Here’s a basic example of using an iframe:

const GameCard = ({ game }) => {
    return (
        <div className="game-card">
            <h3>{game.name}</h3>
            <iframe 
                srcDoc={game.htmlContent}  // Embed the HTML code directly
                style={{ width: '100%', height: '400px', border: 'none' }} 
                title={game.name}
            />
        </div>
    );
};
4. Final Enhancements
You might want to add CSS styles to match the UI of your existing games (Slots, Dice, Plinko).
Update the game management APIs to correctly handle newly added games by their HTML content.
Ensure that your server accepts and processes the HTML effectively without security vulnerabilities.